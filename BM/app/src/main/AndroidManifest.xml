<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bm">

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".selectWhichViewPage"/>
        <activity android:name=".settingsPage" />
        <activity android:name=".incomeInputScreen" />
        <activity android:name=".expenseInputScreen" />
        <activity android:name=".developerInfoPage" />
        <activity android:name=".viewSpendingDelta" />
        <activity android:name=".viewRecentBudgetActivity" />
        <activity android:name=".setIncomeCategories" />
        <activity android:name=".setExpenseCategories" />
        <activity android:name=".splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".homePageActivity"
            android:label="HomePageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".signUpActivity" />
        <activity android:name=".mainActivity" />
    </application>

</manifest>
