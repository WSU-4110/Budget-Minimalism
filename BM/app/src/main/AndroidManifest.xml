<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".selectWhichViewPage"></activity>
        <activity android:name=".settingsPage" />
        <activity android:name=".income_inputscreen" />
        <activity android:name=".expense_inputscreen" />
        <activity android:name=".developerInfoPage" />
        <activity android:name=".view_spending_delta" />
        <activity android:name=".view_recent_budget_activity" />
        <activity android:name=".set_income_categories" />
        <activity android:name=".set_expense_categories" />
        <activity android:name=".splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomePageActivity"
            android:label="HomePageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>